;;) Rovio

(libload "rovio-lib")

(de test ()
	(setq rov (new rovio "admin" "r0b0t0" "192.168.1.10" 1))
	(setq m (==> rov grab))
	(writing "image.bin" (bwrite m)))

;; (test)
;; (setq m (reading "image.bin" (bread)))
;; ^ read images
